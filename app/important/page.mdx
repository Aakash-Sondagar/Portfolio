import { Link } from "next-view-transitions";
import { getMetaData, AnimatedName, Small } from "@/components/common";
import { allBlogs } from "@/utils/blog";

export const metadata = getMetaData("Chaos Cabinet", "/important");

## Chaos Cabinet

<AnimatedName />

My curated collection of treasures from the web—libraries, articles, tools, and documents
that I’ve found valuable or inspiring.

<ul className="list-disc pl-5 space-y-1 marker:text-gray-700 dark:marker:dark:text-gray-300">
  {[...allBlogs]
    .filter((blog) => blog.tags?.includes("important"))
    .sort((a, b) => new Date(b.date) - new Date(a.date))
    .map((blog) => (
      <li key={blog.slug}>
        <Link href={`/blog/${blog.slug}`} className="no-underline">
          <>
            <h6 className="pl-1 text-gray-700 dark:text-gray-300 font-medium">
              {blog.title}
            </h6>
            {blog.description && (
              <p className="text-gray-700 dark:text-gray-300 font-light m-0">
                {blog.description}
              </p>
            )}
          </>
        </Link>
        <h5 className="text-zinc-500 dark:text-zinc-400 font-light text-sm">
          {blog.date}
        </h5>
      </li>
    ))}
</ul>
