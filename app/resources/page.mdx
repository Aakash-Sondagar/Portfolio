import { Link } from "next-view-transitions";
import { getMetaData, AnimatedName, Small } from "@/components/common";
import { resourcesList } from "@/utils/content";

export const metadata = getMetaData("Chaos Cabinet", "/resources");

# Chaos Cabinet

<AnimatedName />

A curated collection of digital artifacts — tools, articles, and insights that shape my engineering journey. Each resource here has contributed to my growth and might help yours too.

<ul className="list-disc pl-5 space-y-2 marker:text-gray-700 dark:marker:text-gray-300">
  {[...resourcesList]
    .sort((a, b) => new Date(b.date) - new Date(a.date))
    .map((blog) => (
      <li key={blog.slug} className="transition-all duration-300 hover:translate-x-1">
        <Link href={`/writings/${blog.slug}`} className="no-underline">
          <>
            <h6 className="pl-1 text-gray-700 dark:text-gray-300 font-medium group-hover:text-indigo-500 dark:group-hover:text-indigo-300">
              {blog.title}
            </h6>
            {blog.description && (
              <>
              <p className="text-gray-700 dark:text-gray-300 font-light m-0 text-sm">
                {blog.description}
              </p>
              <div className="flex items-center space-x-2 pl-1">
                <Small>{blog.date}</Small>
                <span className="text-sm text-indigo-500 dark:text-indigo-300 font-medium hover:underline">
                  Read more →
                </span>
              </div>
              </>
            )}
          </>
        </Link>
      </li>
    ))}
</ul>